apiVersion: 1
providers:
  - name: Crypto Stream Dashboards
    orgId: 1
    folder: ""
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      # This is the path INSIDE the docker container where the JSON will be mounted
      path: /var/lib/grafana/dashboards
